[package]
name = "tinc-proto"
version = "0.0.0"
edition = "2021"
publish = false
description = "Wire-protocol parse/format — Rust port of protocol_*.c sscanf/printf bodies"

[dependencies]

[dev-dependencies]
proptest = "1"
