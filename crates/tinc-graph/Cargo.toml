[package]
name = "tinc-graph"
version = "0.0.0"
edition.workspace = true

[dependencies]

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
